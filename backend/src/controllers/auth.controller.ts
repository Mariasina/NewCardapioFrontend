import { Request, Response } from "express";
import { ICreateAdminResponse, ILoginResponse } from "../res/auth.res";


export const createAdmin = async (req: Request, res: Response<ICreateAdminResponse>) => {
    
}


export const userLogin = async (req: Request, res: Response<ILoginResponse>) => {
    
}
